# Architecture Decision Records — `nexus-cli`

> Numbering is local to this repo (independent of the parent `nexus-platform-plan` ADRs). Format follows
> [Michael Nygard's classic template](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions).

| ID | Status | Title |
|---:|:---:|---|
| [ADR-0001](ADR-0001-cli-framework.md) | Accepted | CLI framework: Spectre.Console.Cli over System.CommandLine + Cocona |
| [ADR-0002](ADR-0002-aot-from-day-one.md) | Accepted | Native AOT from day one; partial trim mode |
| [ADR-0003](ADR-0003-three-project-layout.md) | Accepted | 3-project layered solution enforced by NetArchTest |
| [ADR-0004](ADR-0004-vault-token-from-env.md) | Accepted | Auth model: consume operator's `VAULT_TOKEN` from env; no embedded creds |
| [ADR-0005](ADR-0005-dapper-on-aot.md) | Accepted | Dapper + FluentMigrator on AOT paths (future DB I/O) |
| [ADR-0006](ADR-0006-handrolled-vms-yaml-reader.md) | Accepted | Hand-rolled vms.yaml flow-mapping reader (no YamlDotNet) |
| [ADR-0007](ADR-0007-ssh-net-managed-client.md) | Accepted | SSH client: SSH.NET (managed) over ssh.exe shell-out or native libssh |
| [ADR-0008](ADR-0008-kafka-failover-demo-grade-via-ssh.md) | Accepted | Phase 0.F v0.5: `kafka failover` as a demo-grade DR verb via SSH + the kafka CLI scripts (defers per-consumer-group offset translation to v0.5.1; keeps AOT under the 25 MB gate by not adding `Confluent.Kafka`) |
| [ADR-0009](ADR-0009-cluster-adapter-spi-and-extended-demo-spec.md) | Accepted | Phase 0.G v0.6: `IClusterAdapter` SPI for the data-tier verb expansion (one adapter per cluster, SSH-shell-out, no managed DB drivers); System B JSON demo spec extended with `prerequisites` / `expectedExitCode` / `expectedOutputContains` / `observe[]` / `whatProves` (backwards compatible); AOT exit gate raised to ≤30 MB per `nexus-platform-plan` ADR-0024 |
| [ADR-0010](ADR-0010-cluster-adapter-patterns-and-redis-adapter.md) | Accepted | Phase 0.G.1 v0.6.0: cross-adapter patterns + Redis exemplar — scale-out = apply-on-demand against the proven Terraform/Packer graph (`--count N`, unbounded; reserve an IP/MAC range, no idle VMs) + role-aware SSH join (Greg 2026-06-05: "most stable; live add an arbitrary number of nodes with their role"); chaos = on-node `nexus-chaos.sh` helper with idempotent time-boxed auto-heal; backup = engine-native dump → gateway NFS + restore round-trip verify; cli.ps1 + CI size gate 25→30 MB for the 0.G line |
| [ADR-0012](ADR-0012-percona-adapter-galera-proxysql.md) | Accepted | Phase 0.G.3 v0.6.2: `PerconaAdapter` — Galera synchronous multi-primary + ProxySQL-mediated failover. Reuses the ADR-0011 Vault-KV operator-credential model verbatim (`nexus-cluster-admin` MySQL user, password in Vault KV via `INexusVaultClient`); two control planes (PXC `wsrep_%` status + ProxySQL `:6032` admin `runtime_mysql_servers` for the writer/reader hostgroup map); failover = ProxySQL writer failover (stop the hostgroup-10 writer, poll for promotion); backup = `mysqldump --skip-add-locks` (PXC strict_mode prohibits LOCK TABLES); live-verify bugs: ProxySQL SHUNNED-status read, `"inactive".Contains("active")` substring trap, the LOCK-TABLES restore failure |
| [ADR-0011](ADR-0011-mongo-adapter-and-operator-credential-model.md) | Accepted | Phase 0.G.2 v0.6.1: `MongoAdapter` + the Vault-KV operator-credential model for ALL password-auth clusters (Mongo/Percona/Patroni/SQL) — a dedicated least-privilege `nexus-cluster-admin` operator user (clusterMonitor+clusterManager+backup+restore+userAdminAnyDatabase) whose password lives ONLY in Vault KV (`nexus/oltp/mongo/operator-password`) and is fetched at runtime via an optional `INexusVaultClient` plumbed through `ClusterBootstrapper` (mTLS-only Redis/Kafka unaffected; createUser bootstrap reads it via the node's own Vault Agent token — never on disk). Engine specifics: double-quoted `--eval` JS, URI-database-path scopes `mongodump`, `--nsInclude` required for `mongorestore` ns-remap |
| [ADR-0013](ADR-0013-patroni-adapter-postgres-ha.md) | Accepted | Phase 0.G.4 v0.6.3: `PatroniAdapter` — PostgreSQL Patroni HA + etcd DCS (RBAC) + HAProxy leader-routing VIP. Reuses the ADR-0011 Vault-KV operator-credential model verbatim (`nexus-cluster-admin` role, LOGIN CREATEROLE CREATEDB REPLICATION + pg_monitor/read/write-all-data — **not** superuser); three control planes (patronictl + authed etcdctl + the HAProxy VIP `.60`); failover = `patronictl switchover`, RTO measured at the VIP; backup = `pg_dump --no-owner` into an operator-owned verify **database** (not a schema — the data-only grants can't DDL). Live-verify bugs: the missing patroni.yml `ctl:` block (switchover 403 client-cert), patronictl exits 0 on a refused switchover, the restore CREATEDB-not-schema permission, the single-allowed-domain `patroni.nexus.lab` for cert-rotate |
| [ADR-0015](ADR-0015-starrocks-adapter-fe-quorum-be.md) | Accepted | Phase 0.G.6 v0.6.5: `StarRocksAdapter` — the second **analytics-tier** adapter: an MPP MySQL-protocol warehouse (3 FE BDB-JE metadata quorum + 3 BE tablet nodes; `DISTRIBUTED BY HASH BUCKETS` × `replication_num`). Password-auth (root requires a password); reuses the ADR-0011 Vault-KV operator-credential model (`nexus-cluster-admin` granted `cluster_admin`+`db_admin`+`user_admin`, `DEFAULT ROLE ALL`, distinct from built-in `root`). Single control surface = the FE query port `:9030` via `mysql --skip-ssl` (MariaDB-client TLS requirement) with `MYSQL_PWD`; `SHOW FRONTENDS`/`SHOW BACKENDS` report VMnet10-backplane IPs mapped via vms.yaml. failover = **FE leader re-election** (RTO ≈1.5 s); backup = genuine async `BACKUP/RESTORE SNAPSHOT` to the file:// NFS repo (polled to FINISHED); cert-rotate `pki_int/issue/starrocks-server` (one domain all 6) PKCS#8, BE-first/FE-leader-last. First-try-green on all 12 verbs (no live bugs — the `--skip-ssl` + backplane-IP gotchas caught from the infra read) |
| [ADR-0014](ADR-0014-clickhouse-adapter-sharded-keeper.md) | Accepted | Phase 0.G.5 v0.6.4: `ClickHouseAdapter` — the first **analytics-tier** + first **genuinely sharded** adapter: a `Distributed` table over `ReplicatedMergeTree`, 3 shards × 2 replicas, coordinated by a 3-node **ClickHouse Keeper** RAFT quorum (NOT ZooKeeper, x-tier ADR-0028). Password-auth (decided from a live probe — sha256_password over the mTLS wire, `default` loopback-only); reuses the ADR-0011 Vault-KV operator-credential model (`nexus-cluster-admin` ClickHouse user, `GRANT ALL WITH GRANT OPTION`, distinct from the built-in `admin`). Two control planes (`clickhouse-client` :9440 + the Keeper 4lw `mntr` :9181); failover = **Keeper RAFT leader re-election** (RTO ≈ 1.1 s); `topology` populates **Shards** (3×2); backup = native `BACKUP/RESTORE` to the shared-NFS `analytics_backups` Disk; cert-rotate = `pki_int/issue/clickhouse-server` (one domain `clickhouse.nexus.lab` all 9 nodes) with PKCS#8 key conversion. Live-verify bug: `access_management` is not a per-user `SETTINGS` value (Code 115) — `GRANT ALL` confers it for SQL-created users |
| [ADR-0016](ADR-0016-sqlfci-adapter-wsfc-shared-storage.md) | Accepted | Phase 0.G.7 v0.6.6: `SqlFciAdapter` — the **first Windows cluster**: SQL Server **FCI** (2-node WSFC + shared iSCSI LUN at `S:\`; FCI virtual server `sqlfci` @ .16; CNO `sql-fci-cluster` @ .15; 4-node NodeMajority quorum). Two access planes decided from a live probe: WSFC/cluster-resource cmdlets over **plain Windows-SSH** as local nexusadmin (cluster-admin on the local node; the schtasks-context HANG does NOT apply to plain SSH), and FCI T-SQL as the dedicated **`nexus-cluster-admin`** SQL login (ADR-0011 Vault-KV model, `nexus/oltp/sqlserver/operator-password`, `$env:SQLCMDPASSWORD`) — a SQL login sidesteps the schtasks domain-task entirely. No managed `Microsoft.Data.SqlClient` (NetArchTest). failover = `Move-ClusterGroup` (RTO ≈ 4.5 s); backup = `BACKUP DATABASE … WITH COPY_ONLY` to `S:\Backups` + RESTORE-WITH-MOVE round-trip; cert-rotate = **one shared cert on both nodes + a single cluster checkpoint** (a per-node rotate would break failover), issued via the build-host Vault HTTP API + PFX/SFTP (ws2025 has no openssl); scale-out = skip-with-explanation (fixed 2-node FCI) |
| [ADR-0018](ADR-0018-kafka-cluster-adapter-and-acl-authorizer.md) | Accepted | Phase 0.H.7 v0.6.7: per-cluster `KafkaClusterAdapter` (registered twice → `kafka-east` + `kafka-west`; the v0.5 `kafka` adapter stays as the cross-region MM2 DR meta-cluster) + `KafkaEcosystemAdapter` (observe: SR/REST/Connect/ksqlDB/MM2 over systemctl + HTTPS health). **mTLS-only, no `INexusVaultClient`** (like Redis) — operator identity = the broker's own Vault-PKI keystore via `sudo kafka-*.sh --command-config /etc/nexus-kafka/client-ssl.properties` bootstrapped at the VMnet10 SAN; no managed `Confluent.Kafka`. failover = controlled **controller-leader move** (`kafka-metadata-quorum`, RTO ≈ 4.5 s, complements the MM2 DR); backup = topic→`.jsonl`→verify-topic produce/consume round-trip; cert-rotate = node-token `pki_int/issue/kafka-broker` + `kafka-tls-split.sh` **rolling** restart. **Enabled the KRaft `StandardAuthorizer`** (new cold-rebuild-proven `role-overlay-kafka-acl-authorizer.tf`, rolling restart) with `super.users` = all 15 platform principals (6 brokers + 9 ecosystem — omitting the ecosystem clients would deny-by-default break the platform) so `acl` enforces while ordinary app principals stay deny-by-default. CLI-flag trap caught live: console tools take `--consumer.config`/`--producer.config`, not `--command-config`. Zero live bugs; AOT 26.18 MB; 86/86 tests |
| [ADR-0019](ADR-0019-mongo-sharded-adapter.md) | Accepted | Phase 0.N v0.7.1: `MongoShardedAdapter` (ClusterId `mongo-sharded`) — the genuinely-**sharded** MongoDB cluster (3 config-server RS + 2 shard RSes ×3 + 2 mongos routers), distinct from the 0.G.2 `mongo` RS. Two-headed auth, BOTH using the shared keyFile content as the password: `__system`@`local` (SCRAM-SHA-256) for direct mongod RS ops (config 27019 + shards 27018 — the only principal the shard mongods accept), and `nexus-sharded-admin`@`admin` THROUGH a `mongos` (27017) for cluster-level ops (`local` can't be used through mongos). keyFile from Vault KV `nexus/oltp/mongo/keyfile`; nodes classified by name prefix. `topology` **populates Shards** (the sharded showcase); failover = shard-primary `rs.stepDown` per-shard re-election (RTO ≈ 2.8 s); backup = `mongodump`/`mongorestore` through mongos round-trip; acl = config-server admin users via mongos; **cert-rotate = graceful N/A** (no TLS in 0.N v1; mTLS is the deferred 0.N.1 hardening, ADR-0040). Live-caught bug: single-quoted JS in the health mongos query collided with the `--eval '...'` shell quoting. AOT 26.30 MB; 97/97 tests |
| [ADR-0017](ADR-0017-sqlag-adapter-always-on-listener.md) | Accepted | Phase 0.G.7 v0.6.6: `SqlAgAdapter` — SQL Server **Always On AG** + Listener (ClusterId `sqlserver-ag`; AG `nexus-ag`: FCI primary + 2 async standalone replicas; Listener `sql-ag-listener` @ .17:1433 = the LB-tier HA primitive, ADR-0025). Reuses ADR-0016's control plane; the standalone replicas are Windows-auth-only so direct-replica ops (the `ALTER … FAILOVER` on the target + the manual-seed RESTORE/SET HADR) use `-E`. failover = promote-to-sync → `ALTER AVAILABILITY GROUP FAILOVER` → fail back (RTO ≈ 8.2 s); health proves the **Listener strict-TLS** path (`-N` Encrypt+validate → `PRIMARY=SQLFCI`). **Live-caught bug:** `scale-out add` used `SEEDING_MODE=AUTOMATIC`, which fails in this hybrid topology (FCI files on shared `S:\`, replicas on local `C:\` → automatic seeding tries `S:\SQLData\*.mdf` on a node with no `S:\` → `failure_state=Seeding`). Fixed to **MANUAL seeding** (backup → SFTP-ferry the .bak/.trn build-host-mediated → RESTORE WITH MOVE NORECOVERY → SET HADR), mirroring `role-overlay-ag-bootstrap`; added `ISshClient.DownloadBytesAsync`. `scale-out add` is now also the named recovery command for a drifted secondary |
