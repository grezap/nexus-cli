{
  "id": "DEMO-18-redis-backup-take",
  "title": "Redis: take a backup",
  "description": "Snapshots the cluster via BGSAVE on every shard primary; scp's the RDB to the NFS export on nexus-gateway. STUBBED in v0.6.0.",
  "prerequisites": {
    "vmsAlive": ["redis-1", "redis-2", "redis-3", "redis-4", "redis-5", "redis-6", "nexus-gateway"],
    "envVars": ["NEXUS_SSH_KEY"]
  },
  "steps": [
    {
      "command": "nexus backup take redis --tag pre-migration",
      "waitAfterSeconds": 5,
      "expectedExitCode": 0,
      "expectedOutputContains": ["backup take", "destination"],
      "observe": [
        { "where": "stdout", "what": "backup id (uuid-or-similar) · destination URI on the gateway NFS export · size in MiB · duration" },
        { "where": "nexus-gateway:/srv/nfs/backups/redis/", "what": "new .rdb file (one per shard) under <backup-id>/" }
      ]
    }
  ],
  "whatProves": "Per-shard BGSAVE + scp to gateway NFS is recoverable; the backup id is durable across CLI invocations. (Stubbed in v0.6.0; lands in 0.G.1.x.)"
}
