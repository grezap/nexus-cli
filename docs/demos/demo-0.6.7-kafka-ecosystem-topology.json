{
  "id": "demo-0.6.7-kafka-ecosystem-topology",
  "title": "Kafka ecosystem topology: service groups",
  "description": "Groups the 9 nodes by service kind (HA pairs).",
  "prerequisites": {
    "vmsAlive": [
      "schema-registry-1",
      "schema-registry-2",
      "kafka-connect-1",
      "kafka-connect-2",
      "ksqldb-1",
      "ksqldb-2",
      "mm2-1",
      "mm2-2",
      "kafka-rest-1"
    ],
    "envVars": [
      "NEXUS_SSH_KEY",
      "NEXUS_VMS_YAML"
    ],
    "smokeGate": "nexus-infra-kafka smoke-0.H.2 + smoke-0.H.5 ALL GREEN; role-overlay-kafka-acl-authorizer applied"
  },
  "steps": [
    {
      "command": "nexus topology kafka-ecosystem --json",
      "waitAfterSeconds": 1,
      "expectedExitCode": 0,
      "expectedOutputContains": [
        "schema-registry",
        "kafka-connect",
        "ksqldb"
      ],
      "observe": [
        {
          "where": "stdout",
          "what": "one group per service kind, each HA pair listed"
        }
      ]
    }
  ],
  "whatProves": "topology presents the ecosystem as service groups."
}