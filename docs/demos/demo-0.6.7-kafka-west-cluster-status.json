{
  "id": "demo-0.6.7-kafka-west-cluster-status",
  "title": "Kafka west cluster-status: KRaft quorum",
  "description": "Reports the kafka-west KRaft controller quorum (leader + 3 voters) via kafka-metadata-quorum over each broker own mTLS keystore.",
  "prerequisites": {
    "vmsAlive": [
      "kafka-west-1",
      "kafka-west-2",
      "kafka-west-3"
    ],
    "envVars": [
      "NEXUS_SSH_KEY",
      "NEXUS_VMS_YAML"
    ],
    "smokeGate": "nexus-infra-kafka smoke-0.H.2 + smoke-0.H.5 ALL GREEN; role-overlay-kafka-acl-authorizer applied"
  },
  "steps": [
    {
      "command": "nexus status kafka-west --json",
      "waitAfterSeconds": 1,
      "expectedExitCode": 0,
      "expectedOutputContains": [
        "kafka-west-1",
        "controller-leader",
        "alive"
      ],
      "observe": [
        {
          "where": "stdout",
          "what": "3 brokers, one controller-leader, all alive -- the live quorum view"
        }
      ]
    }
  ],
  "whatProves": "status reports the genuine KRaft controller quorum over mTLS (no managed driver)."
}