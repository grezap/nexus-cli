# v0.8.6 — Completeness pass, batch 1 — VERIFICATION

**Status: BUILT + LIVE-VERIFIED + COLD-REBUILD-PROVEN (2026-06-27).** The first batch of
the completion backlog — closing deferred/"planned for vX" gaps INSIDE the CLI (no
external-script hops). No new adapter; three in-CLI gaps closed + one live-caught bug fixed.

- AOT win-x64 **28.10 MB / 30** ([OK]); **272/272 tests**; NetArchTest clean.
- Sealed + tagged `v0.8.6` (commit `0ddf855`); pushed + CI-green 2026-06-27.

## What closed (the batch-1 gaps)

| Gap | Verb | Before | After |
|---|---|---|---|
| #1 | `cluster-status --verbose` | "timings not yet wired (planned v0.2)" | per-component HTTP elapsed captured in `ClusterStatusService` (`TimedAsync`) → `ComponentTimings` on the report → rendered under `if(verbose)` |
| #2 | Redis `acl grant/revoke` | "not implemented … lands in 0.G.1.x" | `ACL SETUSER` / `DELUSER` on **all nodes** (Redis Cluster ACLs are per-node, not replicated) + `ACL SAVE`; `default` protected; `IsSafeAclToken` injection guard |
| #3 | `kafka` meta-cluster | delegated only status/health/topology; pointed at `kafka.ps1` / `kafka-acls.sh on a broker` for the rest | the meta `KafkaAdapter` now **delegates** status/health/topology/backup take+restore/cert-rotate/acl/chaos/CanResizeVm to the two per-region `KafkaClusterAdapter`s and **merges** (WorseOf overall; region-prefixed health; combined backup-id `<east>||<west>`; chaos region-routes; scale-out routes to the per-region ClusterId) |

## Live verification

- **#1** — `nexus cluster-status <cluster> --verbose` renders a real per-component milliseconds line.
- **#2** — on the running 6-node Redis cluster: `acl list` / `grant` (applied cluster-wide) / `revoke` (`DELUSER`) / the protected-`default` refusal all GREEN.
- **#3** — live-verified east+west: status / health (green) / topology / acl / backup take+restore merge; chaos routed to west; cert-rotate delegates correctly (the rotation itself was gated on kafka's then-pending CA rollover — broker couldn't verify the new-root Vault to issue — expected, not a code bug; resolved when the kafka tier was CA-rolled-over to the new root 2026-06-29).

## Live-caught bug fixed

- `KafkaClusterAdapter.BackupRestoreAsync` used `test -s` (exists AND non-empty), so a **valid empty-topic backup** (a 0-byte file) spuriously reported `MISSING-BACKUP` → changed to `test -f`.

## Notes

- The batch-1 changes are adapter/command code + `ClusterStatusService`; no infra `.tf` changed.
- Detail: CHANGELOG `[0.8.6]`; the completion-backlog tracker `project_infra_and_cli_completion`.
