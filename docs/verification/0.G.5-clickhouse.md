# v0.6.4 / Phase 0.G.5 — ClickHouseAdapter verification

> Canon: [ADR-0009](../adr/ADR-0009-cluster-adapter-spi-and-extended-demo-spec.md) (SPI) +
> [ADR-0011](../adr/ADR-0011-mongo-adapter-and-operator-credential-model.md) (the Vault-KV
> operator-credential model, reused) + [ADR-0014](../adr/ADR-0014-clickhouse-adapter-sharded-keeper.md)
> (ClickHouse + Keeper specifics). v0.6.4 exit-gate proof: the first **analytics-tier** and first
> **genuinely sharded** adapter, **live-verified end-to-end against the running `clickhouse` cluster**
> (3 shards × 2 replicas `ch-shard{1,2,3}-rep{1,2}` @ .44–.49 + 3-node ClickHouse Keeper
> `ch-keeper-1/2/3` @ .41–.43), 2026-06-11.

## What shipped

`ClickHouseAdapter` (`src/Nexus.Cli.Adapters/Cluster/ClickHouseAdapter.cs`) implements all of
`IClusterAdapter` via SSH-shell-out to on-node `clickhouse-client` (native TLS `:9440`) + the Keeper
four-letter-word interface (`echo mntr | nc 127.0.0.1 9181`) — **no managed `ClickHouse.Client`
driver** (NetArchTest-enforced). It authenticates as the dedicated `nexus-cluster-admin` ClickHouse
user (`sha256_password`, `GRANT ALL ON *.* WITH GRANT OPTION`, distinct from the engine's built-in
`admin`); that password comes from **Vault KV** (`nexus/analytics/clickhouse/operator-password`) via
`INexusVaultClient` (ADR-0011 model, unchanged framework). Registered in `ClusterBootstrapper`. AOT
win-x64 **24.84 MB / 30 MB** (+0.66 MB over patroni); **71/71** tests.

## Auth-model decision (from a live probe, not assumption)

ClickHouse is **password-auth**, not mTLS-only like Redis. The wire is mTLS (`:9440`,
`verificationMode relaxed`) but SQL users authenticate by `sha256_password`, and the `default` user
is loopback-only (127.0.0.1/::1) — so every networked client must present a password. → reuse the
LOCKED Vault-KV operator-credential model: a dedicated `nexus-cluster-admin` operator whose password
lives only in Vault KV. The existing `admin`/`app` users (also sha256, also Vault-KV-seeded) stay as
the engine's built-in accounts.

## Two control planes (ADR-0014)

- **SQL / data plane** — `clickhouse-client --secure --accept-invalid-certificate --port 9440` for
  `system.clusters`/`system.replicas`/DDL/RBAC/BACKUP; the Distributed table `nexus.events` over
  `ReplicatedMergeTree` `nexus.events_local`, `internal_replication=true`, 3 shards × 2 replicas.
- **Coordination plane (Keeper)** — `echo mntr | nc 127.0.0.1 9181` → `zk_server_state`
  (leader/follower); the 3-node C++ RAFT quorum (NOT ZooKeeper, ADR-0028). The cluster's only
  cluster-wide leader (the data plane is leaderless / every replica writable).

## Bug caught + fixed by live-verify

1. **`access_management` is NOT a per-user `SETTINGS` value** (CH 26.5 → `Code 115 … neither a
   builtin setting nor … 'SQL_' …`, caught while creating the operator). For a **SQL-created** user
   the access-management capability comes from the **`GRANT ALL` privilege group**, not the XML-only
   `access_management` setting. Fix: create the operator with no SETTINGS clause + `GRANT ALL ON *.*
   WITH GRANT OPTION` — verified to confer `CREATE USER`/`CREATE ROLE ON CLUSTER`. (The thorough
   up-front study of the schema-bootstrap / TLS / backup overlays kept the remaining verbs
   first-try-green — the established Redis/Mongo/Percona/Patroni shapes carried over cleanly.)

## Live evidence (2026-06-11, against the running `clickhouse` cluster)

| Verb | Result | Evidence |
|---|---|---|
| `status clickhouse` | ✅ | 6 data nodes `replica` (shard 1/2/3 ×2) alive · 3 keepers, `ch-keeper-2`=`keeper-leader` · leader=ch-keeper-2 · green |
| `health clickhouse` | ✅ | keeper-quorum 3/3 + leader · 6× server-active · **operator-auth=nexus-cluster-admin** · **distributed-membership 6 host rows** · **distributed-query 600 rows** · 6× replica-health (0 ro/expired, 0 s delay) → green |
| `topology clickhouse` | ✅ | 9-node table + **3 shards × 2 replicas** (Shards populated, per-shard primary = is_leader replica) |
| `acl clickhouse list` | ✅ | admin (ALL) · app · default (ALL) · **nexus-cluster-admin (ALL)** |
| `acl clickhouse grant --user demo_ingest --permissions SELECT` | ✅ | `CREATE USER … ON CLUSTER` + `GRANT SELECT ON nexus.* ON CLUSTER`, then describe (cleaned up after) |
| `failover-test cluster clickhouse` | ✅ | Keeper leader `ch-keeper-2` → `ch-keeper-1` re-elected, **RTO ≈ 1.1 s** (mntr re-election delta), stopped node rejoined as follower (recovered) |
| `backup take clickhouse --tag verify` | ✅ | native `BACKUP TABLE nexus.events_local TO Disk('analytics_backups', …)` on ch-shard1-rep1, **5.5 KiB**, `BACKUP_CREATED` |
| `backup restore clickhouse <id>` | ✅ | `RESTORE TABLE … AS nexus.events_restore_verify FROM Disk(…)`, **items restored = 211** (shard1's local slice; `{uuid}` zk path → no REPLICA_ALREADY_EXISTS) |
| `cert-rotate clickhouse` | ✅ | all **9** nodes (6 data + 3 keeper) re-issued fresh serials via `pki_int/issue/clickhouse-server` (domain `clickhouse.nexus.lab`), PKCS#8 key, intermediate+root ca.crt, rolling restart, **data first / Keeper leader last**, no errors |
| `scale-out remove clickhouse ch-shard3-rep2` | ✅ | graceful leave (server stopped; sibling rep1 still serves shard3) |
| `scale-out add clickhouse --role replica` | ✅ | ch-shard3-rep2 rejoined, ReplicatedMergeTree caught up via Keeper (~5 s) |
| `chaos clickhouse process-kill` | ✅ | killed nexus-clickhouse-server on a replica; observed; **recovered to GREEN** (ReplicatedMergeTree/Keeper rejoin) |

All eleven verb invocations re-confirmed against the **AOT native binary** (`nexus.exe`), not just the
JIT build — `JsonDocument` parsing of the Vault issue response, the Vault KV read, the 4lw parse, and
TLS all survive native compilation. Post-`cert-rotate` and post-`chaos` `health` re-confirmed green.

## Infra (mirrors the percona/mongo/patroni operator-user model)

- **nexus-infra-vmware/envs/security** — `role-overlay-vault-clickhouse-creds-seed.tf` **v2**
  (+`operator-password`, sticky-seeded; admin/app left untouched). Applied live (targeted apply of
  the single `vault_clickhouse_creds_seed` resource; the security env has no VMs → safe — verified a
  ClickHouse node's own agent token reads the 32-char operator-password). **No agent-policy change**
  was needed: the existing `nexus-agent-clickhouse-*` policy already wildcard-reads
  `nexus/data/analytics/clickhouse/*`.
- **nexus-infra-analytics/envs/analytics-clickhouse** — new `role-overlay-clickhouse-operator-user.tf`
  (read operator-password on the DDL coordinator via the agent token → distributed-DDL readiness gate
  → idempotent `CREATE USER … ON CLUSTER` + `GRANT ALL WITH GRANT OPTION` → verify the operator auths
  + manages access) + var `enable_clickhouse_operator_user`. The operator user was created on the
  running cluster via the overlay's exact, idempotent SQL over SSH (non-destructive) so adapter
  live-verification could proceed; it is `ON CLUSTER` (present on all 6 data nodes) and authenticates
  over the native-TLS wire.

## Cold-rebuild proof — ✅ PROVEN (2026-06-11, operator consent)

The `analytics-clickhouse` terraform state carried the **stale x86 `vmrun_path`** (variables.tf
default = `C:/Program Files (x86)/…/vmrun.exe`) in its clone_vm/power_on triggers — a plain `apply`
would plan to replace clone_vm and **delete the .vmx** (see
`feedback_stale_vmrun_path_in_clone_vm_state`). The clean path is a from-zero **cold-rebuild**, which
(a) bakes the correct non-x86 vmrun_path into fresh state, and (b) proves the new
`role-overlay-clickhouse-operator-user.tf` + the creds-seed v2 operator-password in the apply graph.

Done with operator consent 2026-06-11. **Durable fix:** `variables.tf` `vmrun_path` default updated
x86 → `C:/Program Files/VMware/VMware Workstation/vmrun.exe` (the non-x86 canon the cold-rebuilt oltp
envs use), so the fresh apply's clone_vm captures the correct path. Sequence (mirrors the Patroni
cold-rebuild): powered off the 9 VMs cleanly via the correct vmrun path (avoiding the
orphan-running-VM trap the stale destroy-provisioner would otherwise cause — the clone_vm destroy
uses `self.triggers.vmrun` = the stale state path, but the `Remove-Item` filesystem catch-all + `exit
0` clean the dirs *only if the VMs are already stopped*) → `terraform destroy` (50 resources, clean)
→ `terraform apply` from the existing keeper+server templates → `smoke-0.G.5.ps1`. Evidence:

- Clones ran with the **correct non-x86 vmrun path** (no "term not recognized" error); fresh state
  baked the correct path (trap retired for this cluster).
- The full overlay graph ran in-graph: TLS → keeper-config → server-config → **schema-bootstrap EXIT
  GATE GREEN** (sharded × 3 AND replicated × 2, Distributed `count()`=600) → **backup-repo cross-node
  BACKUP/RESTORE round-trip GREEN** → **operator-user EXIT GATE GREEN** ("nexus-cluster-admin
  authenticates + manages access ON CLUSTER nexus_analytics").
- `smoke-0.G.5.ps1` — **ALL 0.G.5 SMOKE CHECKS PASSED** on the rebuilt cluster.
- The verb matrix re-ran GREEN against the fresh cluster (TL=1): status (6 replicas + 3 keeper,
  ch-keeper-1 leader) · health overall=green (operator-auth as nexus-cluster-admin · distributed
  membership 6 · distributed query 600) · topology (Shards 3×2) · failover (Keeper re-election
  ch-keeper-1→ch-keeper-3, recovered) · backup take/restore (206 rows) · scale-out remove+add
  (rejoined) · acl grant · cert-rotate (9 nodes, 0 unchanged) · chaos (recovered to GREEN).

**2nd bug — surfaced by the cold-rebuild (latent IaC ordering, orthogonal to the adapter):** the
`role-overlay-clickhouse-operator-user.tf` (create the operator) and
`role-overlay-clickhouse-backup-repo.tf` (mount the NFS Disk + `systemctl restart
nexus-clickhouse-server` on all 6 data nodes) both `depends_on` only the schema-bootstrap, so
Terraform ran them **in parallel** — the backup-repo's server restarts killed the operator-user's
`clickhouse-client` mid-DDL on the coordinator (`rc=138`). The live (already-stable) cluster hid this
because the operator was created by hand after everything settled. Fix: the operator-user overlay now
`depends_on` **backup-repo too** (operator_v → "2"), so the operator is created only once the
backup-repo restarts have settled. Re-applied → operator-user EXIT GATE GREEN; the full verb matrix
green. (Same flavor as the Patroni cold-rebuild's HAProxy-chroot 5th bug — a latent IaC defect a warm
cluster never exercises.)
